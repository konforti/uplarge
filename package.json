{
  "name": "uplarge",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel ./example/index.html --public-url ./ --no-cache",
    "build": "rm -rf dist; parcel build ./example/index.html --public-url ./ --no-cache",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "parcel": "^2.12.0",
    "gh-pages": "^6.0.0"
  },
  "keywords": []
}
